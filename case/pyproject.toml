[project]
name = "case"
version = "0.1.0"
# We can't use python >= 3.14 because cadquery-ocp doesn't support that version yet.
requires-python = ">= 3.13"
dependencies = [
    "build123d==0.10.0",
    "jupyter_console==6.6.3",
    "ocp_vscode==3.0.1"
]
classifiers = ["Private :: Do Not Upload"]
